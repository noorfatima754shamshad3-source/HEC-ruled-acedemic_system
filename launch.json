{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run AcademicSystem",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/AcademicSystem.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": true,
            "MIMode": "gdb",
            "miDebuggerPath": "gdb.exe",
            "setupCommands": [
                {
                    "description": "Enable pretty-printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ],
            "preLaunchTask": "build"
        }
    ]
}